plugins {
    id 'java-library'
    id 'org.jetbrains.kotlin.jvm'
}

dependencies {
    implementation project (":domain")

    implementation kotlinLibs.values()
    testImplementation testLibsForAllModules.values()
}